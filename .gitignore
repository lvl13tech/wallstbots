".env"
"supabase.env"

Backend/.env
*.env
# Secrets
Project/config/secrets.json
secrets.json
*.secrets

# Admin utility — reads creds from Backend/.env, must not be committed
reset_admin.py
reset-admin-result.txt

# ── Deploy/push scripts (local use only, not source) ──────────────────────────
*.bat

# ── Local notes & docs ────────────────────────────────────────────────────────
LVL13-CLOUDFLARE-SETUP.txt
TESTER_GUIDE.txt
PROJECT.md
upload-lvl13-result.txt

# ── Local scripts & temp files ────────────────────────────────────────────────
test_key.py
Click
scheduled-tasks-dump.txt
__pycache__/
*.pyc
*.pyo
HANDOFF*.md
WallStBots_*.md

# ── Misc junk ─────────────────────────────────────────────────────────────────
logo.svg

# ── Cloudflare Wrangler local cache (not source; may hold account info) ──
.wrangler/

# ── Throwaway result logs from helper .bat scripts ──
CHECK-RESULT.txt
COMMIT-RESULT.txt
commit-push-log.txt

# ── Stray junk files accidentally created (sentence fragments) ──
/$50k
/50
/Run
